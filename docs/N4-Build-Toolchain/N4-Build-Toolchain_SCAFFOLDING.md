## Basic Domain CRUD Tutorial (Todo List)

### Generate a New Dash-Module

Generate a new Dash-module named `TodoList` in the `Example` namespace. This will create all necessary files and boilerplate code for a CRUD module.

```
sail artisan make:dash-module Example TodoList --force
```

> **Note**: The `--force` flag will overwrite existing files if they exist.


